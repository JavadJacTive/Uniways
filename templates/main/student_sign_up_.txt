{% load static %}
<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>فرم ثبت‌نام</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/select2@4.0.13/dist/css/select2.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.0.13/dist/js/select2.min.js"></script>

    <style>
        body {
            background: linear-gradient(120deg, #4b1e3f, #761753); /* گرادیانت زنده */
            color: white;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            font-family: 'Tahoma', sans-serif;
            direction: rtl;
        }

        .login-container {
            max-width: 400px;
            width: 100%;
            margin: 100px auto;
            padding: 20px;
            background: #622a51;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }

        .button-container {
            text-align: center;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .timer-button {
            width: 50%;
            padding: 12px 24px;
            background-color: #8e44ad;
            border: none;
            border-radius: 8px;
            font-size: 1.2em;
            color: #fff;
            cursor: pointer;
            transition: background-color 0.3s;
            min-width: 50px;
            max-height: 50px;
            margin-bottom: 2px;
        }

        .timer-button:hover {
            background-color: #732d91;
        }

        .timer-button:disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }

        .input-field {
            padding: 10px;
            font-size: 1em;
            border: 1px solid #ccc;
            border-radius: 5px;
            width: 50%;
        }

        .form-label {
            text-align: right;
            display: block;
            direction: rtl;
            font-size: 1.1em;
            margin-bottom: 5px;
        }

        .form-control {
            text-align: right;
            direction: rtl;
            border-radius: 8px;
            padding: 10px;
            font-size: 1em;
            border: 1px solid #ccc;
            margin-bottom: 15px;
            background-color: #fff;
        }

        .form-select {
            text-align: right;
            direction: rtl;
            border-radius: 8px;
            padding: 10px;
            font-size: 1em;
            margin-right: 5px;
            margin-bottom: 15px;
            background-color: #fff;
        }

        .btn-primary {
            background-color: #8c3a67;
            border: none;
            padding: 12px;
            border-radius: 8px;
            font-size: 1.1em;
            width: 100%;
            transition: background-color 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #a84e7f;
        }

        #department option {
            background-color: #e0b3ff;
            color: #8200aa9f;
        }

        #department {
            text-align: right;
            display: block;
            direction: rtl;
        }

        .select2-container .select2-dropdown {
            background-color: #761753 !important;
            direction: rtl;
        }

        .select2-container .select2-results__option {
            color: #ffffff !important;
            text-align: right;
        }

        .select2-container .select2-results__option--highlighted {
            background-color: #f8c3fe !important;
            color: #520054 !important;
        }

        h3.text-center {
            margin-bottom: 20px;
            font-size: 1.5em;
        }

        p {
            text-align: center;
            margin-top: 15px;
        }

        p a {
            color: #f8c3fe;
            text-decoration: none;
        }

        p a:hover {
            text-decoration: underline;
        }

        .alert-danger {
            text-align: center;
            margin-bottom: 15px;
        }

        @media (max-width: 576px) {
            .login-container {
                padding: 15px;
                margin: 20px auto;
            }

            .form-control, .form-select {
                font-size: 0.95em;
                padding: 8px;
            }

            .timer-button {
                font-size: 1em;
                padding: 10px;
            }

            .btn-primary {
                font-size: 1em;
                padding: 10px;
            }

            h3.text-center {
                font-size: 1.3em;
            }
        }
    </style>

    <script>
        $(document).ready(function() {
            $("#department").select2({
                placeholder: "یک دانشکده را انتخاب کنید...",
                allowClear: true
            });
        });

        $(document).ready(function() {
            $("#field_of_study").select2({
                placeholder: "یک رشته را انتخاب کنید...",
                allowClear: true
            });
        });

        function startTimer() {
            let button = document.getElementById("timerButton");
            let timeLeft = 10;
            button.disabled = true;
            button.classList.add("disabled");

            fetch("{% url 'student_sms_signUp_Name' %}", {
                method: "POST",
                headers: {
                    "X-CSRFToken": "{{ csrf_token }}",
                    "Content-Type": "application/json"
                },
                body: JSON.stringify({})
            })
            .then(response => response.json())
            .then(data => {
                alert(data.message);
            })
            .catch(error => {
                console.error("Error:", error);
            });

            let timerInterval = setInterval(() => {
                let minutes = Math.floor(timeLeft / 60);
                let seconds = timeLeft % 60;
                button.innerText = `${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
                timeLeft--;

                if (timeLeft < 0) {
                    clearInterval(timerInterval);
                    button.disabled = false;
                    button.classList.remove("disabled");
                    button.innerText = "کد تایید";
                }
            }, 1000);
        }

        $("#department").on("change", function() {
            console.log("Selected department:", $(this).val());
        });
    </script>
</head>
<body>
    <div class="container">
        <div class="login-container">
            {% if form.password.errors %}
                {% for error in form.password.errors %}
                    {% if "This field is required" in error %}
                        <p class="alert alert-danger">فیلد رمز عبور خالیه</p>
                    {% endif %}
                {% endfor %}
            {% endif %}

            <h3 class="text-center">دانشجوی گل (ثبت‌نام)</h3>

            <form method="POST">
                {% csrf_token %}
                <div class="mb-3">
                    <label for="name" class="form-label">نام</label>
                    {% if form.name.errors %}
                        {% for error in form.name.errors %}
                            <p style="text-align: right; color: #ff0000;">{{ error|safe }}</p>
                        {% endfor %}
                    {% endif %}
                    <input type="text" id="name" name="name" class="form-control">
                </div>

                <div class="mb-3">
                    <label for="number" class="form-label">شماره موبایل</label>
                    {% if form.number.errors %}
                        {% for error in form.number.errors %}
                            <div style="text-align: right; color: #ff0000;">{{ error|safe }}</div>
                        {% endfor %}
                    {% endif %}
                    <input type="number" id="number" name="number" class="form-control">
                </div>

                <div class="mb-3">
                    <label for="email" class="form-label">ایمیل <span>(برای اطلاع‌رسانی)</span></label>
                    {% if form.email.errors %}
                        {% for error in form.email.errors %}
                            <p style="text-align: right; color: #ff0000;">{{ error|safe }}</p>
                        {% endfor %}
                    {% endif %}
                    <input type="email" id="email" name="email" class="form-control" oninvalid="this.setCustomValidity('فرمت ایمیل اشتباه است! لطفاً یک ایمیل معتبر وارد کنید.')" oninput="this.setCustomValidity('')">
                </div>

                <div class="mb-3">
                    <label for="department" class="form-label">دانشکده</label>
                    {% if form.department.errors %}
                        {% for error in form.department.errors %}
                            <p style="text-align: right; color: #ff0000;">{{ error|safe }}</p>
                        {% endfor %}
                    {% endif %}
                    <select id="department" name="department" class="form-select">
                        <option value="">🔽 لطفاً یک دانشکده انتخاب کنید</option>
                        {% for department in departments %}
                            <option value="{{ department.id }}">{{ department.name }}</option>
                        {% endfor %}
                    </select>
                </div>

                <div class="mb-3">
                    <label for="field_of_study" class="form-label">رشته تحصیلی</label>
                    {% if form.field_of_study.errors %}
                        {% for error in form.field_of_study.errors %}
                            <p style="text-align: right; color: #ff0000;">{{ error|safe }}</p>
                        {% endfor %}
                    {% endif %}
                    <select id="field_of_study" name="field_of_study" class="form-select">
                        <option value="">🔽 لطفاً یک رشته را انتخاب کنید</option>
                        {% for field in studyField %}
                            <option value="{{ field.id }}">{{ field.name }}</option>
                        {% endfor %}
                    </select>
                </div>

                <div class="mb-3">
                    <label for="password" class="form-label">رمز عبور</label>
                    {% if form.password.errors %}
                        {% for error in form.password.errors %}
                            <p style="text-align: right; color: #ff0000;">{{ error|safe }}</p>
                        {% endfor %}
                    {% endif %}
                    <input type="password" id="password" name="password" class="form-control">
                </div>

                <button type="submit" class="btn btn-primary">بخش بعدی</button>
            </form>

            <p>از قبل ثبت‌نام کردی؟ پس (<a href="/student/login/">ورود</a>)</p>
        </div>
    </div>
</body>
</html>